import styles from './Scroller.module.css';

const Categories = () => {
  return (
    <nav className={styles.categories}>
      <ul>
        <li>홈</li>
      </ul>
    </nav>
  );
};

export default Categories;
